# Runtime dependencies of the notebook and of scripts/.
# Quarto also needs a Python kernel: ipykernel provides the `python3` kernelspec.
numpy>=2.0
pandas>=2.2
matplotlib>=3.9
scikit-learn>=1.6
ipykernel>=6.29
